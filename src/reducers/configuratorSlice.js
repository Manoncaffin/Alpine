import { createSlice } from '@reduxjs/toolkit';

const configuratorSlice = createSlice({
  name: 'configurator',
  initialState: {
    // Initial state here
  },
  reducers: {
    // Reducers here
  },
});

export const { /* Action creators */ } = configuratorSlice.actions;
export default configuratorSlice.reducer;